import DeviceActivity
import SwiftUI

extension DeviceActivityReport.Context {
    static let payMeTimeApplications = Self("Pay Me Time Applications")
    static let payMeTimeAnalyticsMilestone = Self("Pay Me Time Analytics Milestone")
    static let payMeTimeProgress = Self("Pay Me Time Progress")
}
